import * as S from "./todoStyle";
import Form from "../../components/todo/form/Form";

const Todo = () => {
  return (
    <S.TodoContainer>
      <Form />
    </S.TodoContainer>
  );
};

export default Todo;
